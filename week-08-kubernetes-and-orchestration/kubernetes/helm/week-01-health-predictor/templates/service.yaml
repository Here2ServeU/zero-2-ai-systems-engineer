apiVersion: v1
kind: Service
metadata:
  name: {{ include "week-01-health-predictor.fullname" . }}
  labels:
    {{- include "week-01-health-predictor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "week-01-health-predictor.selectorLabels" . | nindent 4 }}
