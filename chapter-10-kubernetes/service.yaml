# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nawex-fraud-svc
spec:
  type: NodePort
  selector:
    app: nawex-fraud-api
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
