# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: zero2ai-fraud-svc
spec:
  type: NodePort
  selector:
    app: zero2ai-fraud-api
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
