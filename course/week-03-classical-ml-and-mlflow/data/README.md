# Week 3 Data

Two synthetic CSVs are expected here; the chapter generates them in its lab:

- `transactions.csv` — synthetic transaction stream with an `is_fraud` label.
- `vitals.csv` — synthetic patient vitals with an `is_anomaly` label.

Generation scripts ship with Chapter 4. Real data should never be committed.
